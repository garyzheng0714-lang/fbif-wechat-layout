# 删除自定义菜单

**接口英文名**：deleteMenu

删除当前自定义菜单。**会同时删除默认菜单和所有个性化菜单。**

## 调用方式

```
GET https://api.weixin.qq.com/cgi-bin/menu/delete?access_token=ACCESS_TOKEN
```

## 返回示例

```json
{ "errcode": 0, "errmsg": "ok" }
```
